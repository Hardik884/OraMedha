/**
 * Business Brain — Achievement Engine barrel.
 *
 * Measured improvements against this clinic's own normal. A sibling of the
 * Constraint Engine: the positive reading of the same kind of evidence, derived
 * from baselines rather than from diagnoses, and depending on neither.
 */
export * from "./achievement-catalog";
export * from "./achievement-engine";

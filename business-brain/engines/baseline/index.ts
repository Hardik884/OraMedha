/**
 * Business Brain — Baseline Engine barrel.
 *
 * What is normal for THIS clinic, per metric, from its own measured history.
 * A band rather than a limit; global benchmarks stay in `signal-thresholds.ts`.
 */
export * from "./baseline-engine";
